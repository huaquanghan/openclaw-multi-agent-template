# .context

Cache fetched API docs, references, or scratch context files here.

Examples:
- `chub get <api-id>` output
- copied snippets from official documentation
- temporary research notes used during implementation
